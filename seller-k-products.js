import { initializeApp } from "https://www.gstatic.com/firebasejs/12.12.0/firebase-app.js";
import { getAuth, signOut, onAuthStateChanged } from "https://www.gstatic.com/firebasejs/12.12.0/firebase-auth.js";
const firebaseConfig = { apiKey: "AIzaSyDqdzlXTddvoBYWaVbTM7_ERO_rUGWjIgE", authDomain: "kng-inventory.firebaseapp.com", projectId: "kng-inventory", storageBucket: "kng-inventory.firebasestorage.app", messagingSenderId: "647181899026", appId: "1:647181899026:web:7cd3b62a7a10771b204fcb", measurementId: "G-5VYMDB59XD" };
const app = initializeApp(firebaseConfig); const auth = getAuth(app);
var formatCurrency = function(n) { return new Intl.NumberFormat('ko-KR', { style: 'currency', currency: 'KRW' }).format(n); };
function escapeHtml(str) { if (str === null || str === undefined) return ''; return String(str).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;').replace(/'/g, '&#039;'); }
function showToast(message, type) { if (!type) type = 'info'; var container = document.getElementById('toastContainer'); if (!container) return; var icons = { success: 'bx-check-circle', error: 'bx-error-circle', warning: 'bx-error', info: 'bx-info-circle' }; var toast = document.createElement('div'); toast.className = 'toast ' + type; toast.innerHTML = "<i class='bx " + (icons[type] || icons.info) + "'></i> <span>" + escapeHtml(message) + "</span>"; container.appendChild(toast); setTimeout(function() { toast.classList.add('fade-out'); setTimeout(function() { toast.remove(); }, 300); }, 3000); }
function updateConnectionStatus(online) { var statusEl = document.getElementById('firebaseStatus'); if (!statusEl) return; if (online) { statusEl.innerHTML = "<i class='bx bx-data'></i> NAS API 연결됨"; statusEl.style.color = 'var(--success)'; } else { statusEl.innerHTML = "<i class='bx bx-loader-alt bx-spin'></i> 연결 중..."; statusEl.style.color = 'var(--warning)'; } }
const API_BASE = 'http://localhost:3000/api/seller-k/products';
function loadProducts() { updateConnectionStatus(false); fetch(API_BASE).then(res => res.json()).then(data => { products = data || []; renderTable(); updateConnectionStatus(true); }).catch(err => { console.error('API Error:', err); showToast('데이터를 불러오는데 실패했습니다.', 'error'); updateConnectionStatus(false); document.getElementById('skTableBody').innerHTML = '<tr><td colspan="15" style="text-align:center; padding:30px;">API 서버 연결 실패</td></tr>'; }); }
function generateId() { return 'sk_' + Date.now() + '_' + Math.random().toString(36).substr(2, 5); }
var products = []; var editingId = null;
function calcCommission(sellPrice, sellShipping) { var baseExt = sellPrice + sellShipping; var orderFee = Math.round(baseExt * 0.0363); var salesFee = Math.round(sellPrice * 0.03); return orderFee + salesFee; }
function calcBuyTotal(buyPrice, buyShipping, shippingBasis, shippingQty) { if (!buyPrice) buyPrice = 0; if (!buyShipping) buyShipping = 0; var finalShipping = 0; if (shippingBasis === '무료') { finalShipping = 0; } else if (shippingBasis === '조건부' || shippingBasis === '유료') { finalShipping = buyShipping; } else { var sq = parseInt(shippingQty, 10); if (isNaN(sq) || sq < 1) sq = 1; finalShipping = Math.round(buyShipping / sq); } return buyPrice + finalShipping; }
function calcSellTotal(sellPrice, sellShipping) { return (sellPrice || 0) + (sellShipping || 0); }
function calcProfit(buyTotalVATExclusive, sellTotalVATInclusive, commission) { var buyTotalVATInclusive = Math.round(buyTotalVATExclusive * 1.1); return sellTotalVATInclusive - buyTotalVATInclusive - commission; }
function calcProfitRate(profit, sellTotal) { if (!sellTotal || sellTotal === 0) return 0; return (profit / sellTotal) * 100; }
function renderTable() {
    var tbody = document.getElementById('skTableBody'); if (!tbody) return; var totalStock = products.length; var totalBuy = 0; var totalSell = 0; var totalProfit = 0; var html = '';
    if (products.length === 0) { html = '<tr><td colspan="15" style="text-align:center; padding:30px; color:var(--text-light);">등록된 매입상품이 없습니다.</td></tr>'; } else {
        products.forEach(function(p) { var buyTotal = calcBuyTotal(p.buyPrice, p.buyShipping, p.shippingBasis, p.shippingQty); var sellTotal = calcSellTotal(p.sellPrice, p.sellShipping); var commission = calcCommission(p.sellPrice || 0, p.sellShipping || 0); var profit = calcProfit(buyTotal, sellTotal, commission); var profitRate = calcProfitRate(profit, sellTotal); totalBuy += buyTotal; totalSell += sellTotal; totalProfit += profit;
            var shippingBasisLabel = p.shippingBasis; if (p.shippingBasis === '수량별') shippingBasisLabel += ' (' + p.shippingQty + '개당)';
            html += "<tr><td class="col-check"><input type="checkbox" class="sk-checkbox" value="" + p.id + ""></td><td>" + escapeHtml(p.supplier) + "</td><td>" + escapeHtml(p.brand) + "</td><td><strong>" + escapeHtml(p.name) + "</strong></td><td>" + escapeHtml(p.color) + "</td><td>" + escapeHtml(p.size) + "</td><td>" + escapeHtml(p.uploadDate) + "</td><td class="col-num buy-col">" + formatCurrency(p.buyPrice) + "</td><td class="col-num buy-col">" + formatCurrency(p.buyShipping) + "</td><td class="buy-col" style="text-align:center; font-size:12px;">" + escapeHtml(shippingBasisLabel) + "</td><td class="col-num buy-col" style="font-weight:600;">" + formatCurrency(buyTotal) + "</td><td class="col-num sell-col">" + formatCurrency(p.sellPrice) + "</td><td class="col-num sell-col">" + formatCurrency(p.sellShipping) + "</td><td class="col-num sell-col" style="font-weight:600;">" + formatCurrency(sellTotal) + "</td><td class="col-num profit-col" style="color:var(--danger)">" + formatCurrency(commission) + "</td><td class="col-num profit-col " + (profit > 0 ? 'text-success' : (profit < 0 ? 'text-danger' : '')) + "" style="font-weight:bold;">" + formatCurrency(profit) + "</td><td class="col-num profit-col"><span class="badge " + (profitRate > 20 ? 'badge-success' : 'badge-neutral') + "">" + profitRate.toFixed(1) + "%</span></td><td class="col-action"><button class="btn-icon edit-btn" data-id="" + p.id + ""><i class='bx bx-edit-alt'></i></button></td></tr>";
        }); }
    tbody.innerHTML = html; document.getElementById('skTotalCount').textContent = totalStock; document.getElementById('skTotalBuy').textContent = formatCurrency(totalBuy); document.getElementById('skTotalSell').textContent = formatCurrency(totalSell); document.getElementById('skTotalProfit').textContent = formatCurrency(totalProfit);
    document.querySelectorAll('.edit-btn').forEach(function(btn) { btn.addEventListener('click', function() { openModal(this.getAttribute('data-id')); }); });
}
function openModal(id) { var modal = document.getElementById('skModal'); if (!modal) return; editingId = id; document.getElementById('skModalTitle').textContent = id ? '매입상품 수정' : '신규 매입상품 추가'; document.getElementById('skForm').reset(); document.getElementById('skUploadDate').value = new Date().toISOString().split('T')[0]; document.getElementById('skShippingQty').value = "1"; toggleShippingQty(); updateCalcPreview();
    if (id) { var p = products.find(function(i) { return i.id === id; }); if (p) { document.getElementById('skSupplier').value = p.supplier || ''; document.getElementById('skBrand').value = p.brand || ''; document.getElementById('skName').value = p.name || ''; document.getElementById('skColor').value = p.color || ''; document.getElementById('skSize').value = p.size || ''; document.getElementById('skUploadDate').value = p.uploadDate || ''; document.getElementById('skBuyPrice').value = p.buyPrice || ''; document.getElementById('skBuyShipping').value = p.buyShipping || ''; document.getElementById('skShippingBasis').value = p.shippingBasis || '수량별'; document.getElementById('skShippingQty').value = p.shippingQty || '1'; document.getElementById('skSellPrice').value = p.sellPrice || ''; document.getElementById('skSellShipping').value = p.sellShipping || ''; toggleShippingQty(); updateCalcPreview(); } }
    modal.classList.add('active'); }
function closeModal() { var modal = document.getElementById('skModal'); if (modal) modal.classList.remove('active'); editingId = null; }
function toggleShippingQty() { var b = document.getElementById('skShippingBasis').value; var wrf = document.getElementById('shippingQtyWrap'); if (b === '수량별') { wrf.classList.remove('hidden'); } else { wrf.classList.add('hidden'); } }
function updateCalcPreview() { var bp = parseInt(document.getElementById('skBuyPrice').value, 10) || 0; var bs = parseInt(document.getElementById('skBuyShipping').value, 10) || 0; var base = document.getElementById('skShippingBasis').value; var qty = parseInt(document.getElementById('skShippingQty').value, 10) || 1; var sp = parseInt(document.getElementById('skSellPrice').value, 10) || 0; var ss = parseInt(document.getElementById('skSellShipping').value, 10) || 0; var buyTotal = calcBuyTotal(bp, bs, base, qty); var sellTotal = calcSellTotal(sp, ss); var commission = calcCommission(sp, ss); var profit = calcProfit(buyTotal, sellTotal, commission); var profitRate = calcProfitRate(profit, sellTotal); document.getElementById('skPreviewCommission').textContent = formatCurrency(commission); var pp = document.getElementById('skPreviewProfit'); pp.textContent = formatCurrency(profit); pp.className = profit > 0 ? 'text-success' : (profit < 0 ? 'text-danger' : ''); document.getElementById('skPreviewRate').textContent = profitRate.toFixed(1) + '%'; }
function setupAuth() { var mainApp = document.getElementById('mainApp'); var logoutBtn = document.getElementById('logoutBtn');
    onAuthStateChanged(auth, function(user) { if (user) { if (mainApp) mainApp.classList.remove('hidden'); loadProducts(); } else { window.location.href = 'index.html'; } });
    if (logoutBtn) { logoutBtn.addEventListener('click', function(e) { e.preventDefault(); signOut(auth).then(function() { window.location.href = 'index.html'; }); }); } }
document.addEventListener('DOMContentLoaded', function() {
    setupAuth();
    document.getElementById('addProductBtn').addEventListener('click', function() { openModal(null); });
    document.getElementById('closeSkModalBtn').addEventListener('click', closeModal); document.getElementById('cancelSkBtn').addEventListener('click', closeModal);
    document.getElementById('skShippingBasis').addEventListener('change', function() { toggleShippingQty(); updateCalcPreview(); });
    ['skBuyPrice', 'skBuyShipping', 'skShippingQty', 'skSellPrice', 'skSellShipping'].forEach(function(id) { document.getElementById(id).addEventListener('input', updateCalcPreview); });
    document.getElementById('skForm').addEventListener('submit', function(e) {
        e.preventDefault(); var data = { supplier: document.getElementById('skSupplier').value.trim(), brand: document.getElementById('skBrand').value.trim(), name: document.getElementById('skName').value.trim(), color: document.getElementById('skColor').value.trim(), size: document.getElementById('skSize').value.trim(), uploadDate: document.getElementById('skUploadDate').value, buyPrice: parseInt(document.getElementById('skBuyPrice').value, 10) || 0, buyShipping: parseInt(document.getElementById('skBuyShipping').value, 10) || 0, shippingBasis: document.getElementById('skShippingBasis').value, shippingQty: parseInt(document.getElementById('skShippingQty').value, 10) || 1, sellPrice: parseInt(document.getElementById('skSellPrice').value, 10) || 0, sellShipping: parseInt(document.getElementById('skSellShipping').value, 10) || 0 };
        if (editingId) { data.id = editingId; fetch(API_BASE + '/' + editingId, { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) }).then(res => res.json()).then(() => { showToast('상품 정보가 수정되었습니다.', 'success'); closeModal(); loadProducts(); }).catch(err => showToast('수정 실패: ' + err.message, 'error')); } else { data.id = generateId(); fetch(API_BASE, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) }).then(res => res.json()).then(() => { showToast('새 상품이 등록되었습니다.', 'success'); closeModal(); loadProducts(); }).catch(err => showToast('등록 실패: ' + err.message, 'error')); } });
    document.getElementById('deleteSkBtn').addEventListener('click', function() { var checked = document.querySelectorAll('.sk-checkbox:checked'); if (checked.length === 0) { showToast('삭제할 상품을 선택해주세요.', 'warning'); return; } if (!confirm('선택한 ' + checked.length + '개 상품을 삭제하시겠습니까?')) return; var idsToDelete = []; checked.forEach(function(cb) { idsToDelete.push(cb.value); }); fetch(API_BASE + '/delete', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ ids: idsToDelete }) }).then(res => res.json()).then(() => { document.getElementById('selectAllSk').checked = false; showToast('삭제했습니다.', 'success'); loadProducts(); }).catch(err => showToast('삭제 실패: ' + err.message, 'error')); });
    document.getElementById('selectAllSk').addEventListener('change', function() { var checked = this.checked; document.querySelectorAll('.sk-checkbox').forEach(function(cb) { cb.checked = checked; }); });
    var t = document.getElementById('sellerKToggle'), g = document.getElementById('sellerKMenuGroup'); if(t&&g) t.addEventListener('click', function(e){ e.preventDefault(); g.classList.toggle('open'); });
    var h = document.getElementById('hamburgerBtn'), s = document.getElementById('sidebar'), o = document.getElementById('sidebarOverlay'); if(h&&s) h.addEventListener('click', function(){ s.classList.toggle('open'); if(o) o.classList.toggle('active'); }); if(o) o.addEventListener('click', function(){ s.classList.remove('open'); o.classList.remove('active'); });
    toggleShippingQty();
});
